{
  "name": "paddock-pal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Auto-tracking equestrian live streaming — capture page, WHEP viewer, Cloudflare Stream session broker.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "protocol:header": "node tools/gen-esp32-header.js",
    "worker:dev": "wrangler dev worker/index.js",
    "worker:deploy": "wrangler deploy"
  },
  "devDependencies": {
    "vite": "^5.4.10",
    "vitest": "^2.1.4",
    "wrangler": "^3.86.0"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.18"
  }
}
